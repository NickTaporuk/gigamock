path: "/users/:userID"
method: DELETE
type: http

scenarious:
  - type: http
    name: "default scenarios"
    request:

    response:
      statusCode: 200
  - type: http
    name: "error 500"
    request:
    response:
      statusCode: 500

