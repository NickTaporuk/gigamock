path: "/users/:userID"
method: PUT
type: http

scenarious:
  - name: "default scenarios"
    request:

    response:
      statusCode: 200
  - name: "error 500"
    request:
    response:
      statusCode: 500

