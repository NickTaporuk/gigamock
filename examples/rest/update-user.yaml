path: "/users/:userID"
method: PUT
type: http

scenarios:
  - name: "default scenarios"
    request:

    response:
      statusCode: 201
  - name: "error 500"
    request:
    response:
      statusCode: 500

